import React from "react";
import { shallow } from "enzyme";
import { getGifs } from "../../helpers/getGifs";

describe("Pruebas", () => {
    // ============ TESTS ============ //
    /*test("Test length", async () => {
        const wrapper = await getGifs("Konosuba");

        expect(wrapper.length).toBe(6);
    });
    */
});
